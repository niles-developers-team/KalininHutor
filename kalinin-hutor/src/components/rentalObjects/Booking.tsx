import { Stack, Typography } from "@mui/material";

export const BookingComponent = function (): JSX.Element {
    return (
        <Stack>
            <Typography variant="h5">Подтверждение бронирования</Typography>
        </Stack>
    );
}